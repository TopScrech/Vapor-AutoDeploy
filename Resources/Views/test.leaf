<!DOCTYPE html>
<html lang="en" class="h-full">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script src="https://cdn.tailwindcss.com"></script>
        <title>abcdefg</title>
    </head>
    <body>
        <table class="w-full">
            <thead class="bg-gray-50 dark:bg-neutral-800">
                <tr>
                    <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 dark:text-neutral-400 uppercase tracking-wider">Text</th>
                    <th class="hidden sm:table-cell px-6 py-4 text-left text-xs font-medium text-gray-500 dark:text-neutral-400 uppercase tracking-wider">ID</th>
                </tr>
            </thead>

            <!-- Parent subscribes to DB model DummyModel (Fluent) -->
            <!-- that means that the client side server message handler will keep state of each row in sync by js -->
            <!-- Source of truth is the server db (Fluent) -->

            <tbody class="divide-y divide-gray-200 dark:divide-neutral-700">
                #for(entry in entries):
                    <tr mist-model="DummyModel" mist-id="#(entry.id)" class="hover:bg-gray-50 dark:hover:bg-neutral-750 transition-colors duration-150">
                        <td class="px-6 py-4 max-w-[160px]">
                            <span class="block text-sm text-gray-700 dark:text-neutral-300 truncate font-medium">#(entry.text)</span>
                        </td>
                        
                        <td class="hidden sm:table-cell px-6 py-4">
                            #(entry.id)
                        </td>
                    </tr>
                #endfor
            </tbody>
        </table>
    </body>
</html>