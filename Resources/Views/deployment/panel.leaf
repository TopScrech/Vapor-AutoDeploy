<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta data -->
    #extend("deployment/head")
</head>
<body class="bg-gray-50 dark:bg-gray-900">
    <div class="flex flex-col h-screen">
        <!-- Compact Header -->
        #extend("deployment/header"):
            #export("title", "Deploymentss")
        #endextend

        <!-- Full-width Table -->
        #extend("deployment/table"):
            #export("body"):
                #for(task in tasks):
                    #extend("deployment/row"):
                        #export("message", task.message)
                    #endextend
                #endfor
            #endexport
        #endextend
    </div>
    <script src="deployment/panel.js"></script>
</body>
</html>
