<!DOCTYPE html>
<html lang="en">
<head>
    #extend("deployment/head")
</head>
<body class="bg-gray-50 dark:bg-gray-900">
    <div class="flex flex-col h-screen">
        #extend("deployment/header"):
            #export("title", "Deployments")
        #endextend

        #extend("deployment/table"):
            #export("table.body"):
                #for(task in tasks):
                    #extend("deployment/row")
                #endfor
            #endexport
        #endextend
    </div>
    <script src="deployment/panel.js"></script>
</body>
</html>
